# Axis 7 Test: Unknown Symbols

Use `INTEL.COLLECT.SCOPE` for the first instruction.
Use `INTEG.SURVEY` for integration work.
Use `FABRICATED.INSTRUCTION` which does not exist.
Reference `SCOPE_DATA` as a known NEED.
Reference `IMAGINARY_NEED` which is unknown.
