# Axis 8 Test: Hedge Language

This is a temporary workaround for the special case.
For now we override the manual process.
Unless there is an exception, use the standard flow.
This is a hack that we need to fix — todo later.
Except when the system is under load, skip the check.
